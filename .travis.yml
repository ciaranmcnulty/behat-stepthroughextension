language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

matrix:
  include:
    - php: 5.6
    - php: 7.0

install:
  - composer update

script:
   - bin/phpspec run --format=pretty
   - bin/behat --format=pretty
